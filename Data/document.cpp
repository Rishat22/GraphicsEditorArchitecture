#include "document.h"

void Document::load(const std::string& text)
{
	/* ToDo add loading shapes */
	std::cout << "load file " << text << std::endl;
}

void Document::saveAs(const std::string& name)
{
	/* ToDo add saving shapes */
	std::cout << "save file " << name << std::endl;
}
